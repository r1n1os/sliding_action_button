enum LoaderButtonEnumStates {
  /// This is the normal view of the button once the screen loads
  initial,
  /// This is the loading of the button once the drag action is completed
  loading,
  /// This is when we don't want to show the button
  gone,
}