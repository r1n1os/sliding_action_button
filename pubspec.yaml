name: sliding_action_button
description: "Sliding action button package provides prebuilt functionality for sliding(swipe) action button with some predefined shapes"
version: 1.0.0
homepage: https://github.com/r1n1os/sliding_action_button
repository: https://github.com/r1n1os/sliding_action_button
issue_tracker: https://github.com/r1n1os/sliding_action_button/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

